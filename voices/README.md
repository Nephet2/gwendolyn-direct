# Voice references

No voice recordings are included. Only use recordings that you made or have permission to use.

Kokoro uses its built-in voice names and needs no file here. Chatterbox users can add a clean WAV and then set the matching path in `director_voices.json` and `config.json`.
